#ifndef STD_BINGINGS_H
#define STD_BINGINGS_H

// Defininions of std libc functions to use
// Change if in need to use custom funcitons instead of standard ones

#define getCharFromFile(filePointer) fgetc(filePointer)

#endif
